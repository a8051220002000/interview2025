# provisioning/datasources/datasource.yml
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://exam-control-plane:30090   # kind svc
    isDefault: true
    editable: true